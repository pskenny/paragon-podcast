<Page
    x:Class="Paragon_Podcast.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Paragon_Podcast"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#FF666666" Width="1280" Height="720">
        
        <!--Meida player-->
        <MediaPlayerElement 
            x:Name="player"
            AutoPlay="False"
            AreTransportControlsEnabled="True" 
            Background="#FF323232">
            <MediaPlayerElement.TransportControls>
                <MediaTransportControls IsCompact = "True" IsFullWindowButtonVisible = "False" IsZoomButtonVisible = "False"  Background="#FF632A2A"/>
            </MediaPlayerElement.TransportControls>
        </MediaPlayerElement>
        
        <!--chanel list pannel-->
        <StackPanel HorizontalAlignment="Left"  VerticalAlignment="Top"  Background="#FCC3C3C3"> 
            <ListView x:Name="LVchannelList" IsItemClickEnabled="True" ItemClick="LVchannelList_ItemClick">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <TextBlock Text="{Binding title}"></TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        
        <!--episode list pannel-->
        <StackPanel HorizontalAlignment="Left"  VerticalAlignment="Top" Background="#FFC3C3C3">
            <ListView x:Name="LVepisodeList" IsItemClickEnabled="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <TextBlock Text="{Binding title}"></TextBlock>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        
        <!--Logo pannel-->
        <StackPanel HorizontalAlignment="Left" Height="144" VerticalAlignment="Top" Width="1022">
            <StackPanel.Background>
                <ImageBrush ImageSource="Assets/Logo.png"/>
            </StackPanel.Background>
        </StackPanel>


    </Grid>
</Page>
